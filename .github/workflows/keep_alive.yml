name: 🔁 Keep Streamlit App Alive

on:
  schedule:
    - cron: "0 */8 * * *"  # Chạy mỗi 8 tiếng
  workflow_dispatch:  # Cho phép chạy thủ công nếu cần

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: 🔁 Curl Streamlit app
        run: curl -L https://ai-health.streamlit.app > /dev/null
